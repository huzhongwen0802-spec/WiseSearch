# ExpertSearch 10 人现场演示版

此分支用于一次性现场演示。演示配置只在启动脚本进程内生效，不修改 `.env`。

## 启动

先停止已经运行的 Streamlit，然后在项目目录执行：

```powershell
.\start_demo_10_experts.cmd
```

浏览器访问：

```text
http://localhost:8501
```

演示模式只运行用户选择的第一个细分领域，最终最多输出 10 位专家，并保留
OpenAlex 和 Tavily 候选证据检索、领域错配清洗、近似去重、基础评分和最终
Excel 下载流程。

为了将现场等待控制在约 3 分钟以内，演示版使用 `gpt-4.1` 完成一次研究员整理，
验证与纠错由 Python 规则接管；关闭 Semantic Scholar、逐人数据库与主页深挖、
OpenCLI、生存状态联网核验、最终全量定向补全和 LLM 交付翻译。未在演示版联网
核验的字段会明确保留为“未执行”或“暂无公开信息”。正式 `main` 分支的完整质量
链路不受影响。

演示版同时使用 Tavily ultra-fast 检索，并把 OpenAlex 请求限制为 10 秒、1 次尝试。
3 分钟是正常网络和模型中转服务可用时的目标耗时；外部服务异常时，研究员 LLM
会在 75 秒终止并返回明确错误，不会像完整生产链路一样进行多轮长时间重试。

## 演示建议

选择一个边界清晰、公开资料较丰富的细分领域。现场演示时不要同时上传大型补充文件，
也不要启用智能体生成细分领域，以减少额外 LLM 调用和等待时间。

## 演示结束后回到开发版

先在运行终端按 `Ctrl + C` 停止演示系统，然后执行：

```powershell
git switch main
.\start_streamlit_logged.cmd
```
